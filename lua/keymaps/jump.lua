-- Alt+j to jump forward in jumplist (like <C-i>)
vim.keymap.set("n", "<A-j>", "<C-i>", { noremap = true, silent = true })

-- Alt+k to jump backward in jumplist (like <C-o>)
vim.keymap.set("n", "<A-k>", "<C-o>", { noremap = true, silent = true })
